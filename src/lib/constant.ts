import { Field } from 'snarkyjs';

export const SMT_DEPTH = 254;
export const SMT_EMPTY_VALUE = Field.zero;
export const RIGHT = true;
export const LEFT = false;
export const ERR_KEY_ALREADY_EMPTY = 'Key already empty';
